# Base URL of the backend API, including the /api suffix.
# Local development:  http://localhost:7000/api
# Production (Netlify env var):  https://<your-render-service>.onrender.com/api
VITE_API_URL=
